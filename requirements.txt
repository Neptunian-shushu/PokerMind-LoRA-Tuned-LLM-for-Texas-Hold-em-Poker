# PokerMind: LoRA-Tuned LLM for Texas Hold'em Poker
# Requirements for CS6220 Project on PACE Cluster
# Note: PyTorch is pre-installed via PACE modules (use: module load anaconda3)

# Hugging Face Ecosystem
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
huggingface-hub>=0.19.0

# LoRA Fine-tuning
peft>=0.6.0

# Quantization (requires CUDA-compatible build)
bitsandbytes>=0.41.0

# Data Processing
numpy
pandas
tqdm

# Visualization (optional - for results analysis)
matplotlib
seaborn

# Note: The following are already available in PACE anaconda3 environment:
# - torch (via CUDA modules)
# - scikit-learn
# - jupyter, notebook, ipykernel
# - scipy

# Installation on PACE:
# 1. module load anaconda3
# 2. pip install --user -r requirements.txt
# 3. For GPU support, ensure CUDA modules are loaded